# Reviews

`/tri-review` 실행 결과 요약을 여기 쌓는다. 파일명: `YYYY-MM-DD-대상.md`.
raw 출력은 저장하지 않는다 — 판정 표(지적 | 출처 | 심각도 | 판정 | 조치)만 남긴다 (`../rules/ai-collaboration.md` 토큰 효율 규칙 6번).
새 리뷰 실행 전 여기서 같은 대상의 기존 리뷰가 있는지 먼저 확인한다 (재리뷰 금지 규칙).
