# Neulsang(늘상) — 로컬 우선 개발자 영어 학습 앱

## 프로젝트 개요
업무 중 마주친 영어 단어·용어·문장을 단축키로 즉시 검색·AI 해석하고, 그 기록을 복습 카드로 전환하는 로컬 우선(local-first) 데스크톱 앱.
**제품명은 Neulsang(한글 "늘상")으로 확정** (`docs/adr/ADR-0006-product-name.md`). 영문은 `Neulsang`, 한글 병기는 "늘상". 컴포넌트·바이너리명(`desktopd` 등)은 제품명과 별개다.

**기준 문서: `docs/prd.md`** (제품 정의, DB 스키마, API, 화면 설계, MVP 범위 — 이미 상세). 충돌 시 그 문서가 우선.
문서 지도: `docs/README.md` / 용어: `docs/glossary.md` / 결정 기록: `docs/adr/`

## 개발 전략 (핵심 원칙)
"작게 설계 → 구현 → 검증 → 확장" 반복. `docs/planning/backlog.md`의 마일스톤(v0.1 ~ v0.5) 순서를 따른다.
모든 기능 판단 기준(PRD §21): **"이 기능이 사용자의 영어 기억을 더 잘 쌓고, 다시 떠올리게 하는가?"** — 아니면 MVP에서 뺀다.

## 팀
1인 개발(dgkwon90). 사람 리뷰어가 없으므로 codex/agy 교차 검토가 실질적인 2차 검증 수단이다.

## 일하는 방식 (상세는 docs/rules/)
- **사이클**: 이슈(1~2일 크기) → 설계 → TDD → 구현 → lint·취약점 검사 → (중요 결정만) codex/agy 교차검토 → 확정 (`rules/development-cycle.md`)
- **작업 관리**: `docs/planning/backlog.md`가 이슈 대장
- **AI·토큰 효율**: tri-review는 ADR급 결정에만, 범위는 파일 단위로 명시, 재리뷰 금지 (`rules/ai-collaboration.md`)
- **용어**: 새 용어는 `docs/glossary.md`에 등재, 같은 개념에 두 단어 금지

## 기술 스택
- Backend sidecar: **Go** (`apps/desktopd`) — HTTP API + SQLite + AI provider 연동 + 복습 스케줄
- Desktop UI: **Tauri 2 + TypeScript + React** (`apps/desktop-ui`) — `docs/adr/ADR-0005-frontend-framework.md`
- 로컬 DB: **SQLite** (WAL 모드, FTS5)
- AI Provider: `Explainer` 인터페이스로 추상화, 1차 실연동 provider는 **Gemini** (`docs/adr/ADR-0004`)

## Repo 구조
```
apps/{desktopd, desktop-ui, api(추후 중앙서버)}   deploy/   scripts/   docs/
```
1. `apps/desktopd/internal/domain/`은 infra(AI provider, DB 드라이버 등)를 직접 알면 안 된다 — interface로 주입 (PRD §18.1)
2. `apps/desktopd` ↔ `apps/desktop-ui`는 직접 import 금지 — 로컬 HTTP API로만 통신 (PRD §15)
3. SQLite 스키마 변경은 migration 파일로만
4. API key 등 secret은 평문 저장·커밋 금지
5. **원격 push는 사용자가 명시 지시할 때만**

## AI 오케스트레이션
Claude가 중심, `.claude/agents/`의 codex-worker(구현 위임)·agy-worker(대량 분석)를 작업자로. 위임 결과는 diff/파일 직접 검증 후 채택. `--dangerously-*` 플래그는 사용자가 해당 대화에서 명시 허락 시만. 되돌리기 어려운 결정만 `/tri-review`.

## 현재 상태 (2026-07-12)
설계 문서·ADR·백로그 준비 완료. 원격 저장소 `github.com/dgkwon90/dictionary`(public) 연결·push 완료.
착수 전 확정 필요 항목 모두 결정됨: 제품명=**Neulsang**(ADR-0006), AI provider=**Gemini**(ADR-0004), UI=**React**(ADR-0005).
백로그 #1~#5 완료: `apps/desktopd`(module `neulsang/desktopd`) — `GET /healthz`, `POST /v1/captures`(PRD §15.1), capture 생성 직후 `Explainer` 실행(비동기 goroutine, 90s 타임아웃) → `GET /v1/captures/{id}/explanation`(PRD §15.2), `GET /v1/inbox`(PRD §15.3, status/limit 필터)+`POST /v1/inbox/{id}/save|archive`. Inbox 상태는 captures.inbox_status(new/saved/archived만 저장) + lookup_jobs.status/review_cards 존재 여부로 review_added·failed를 조회 시 파생(ADR-0007). SQLite 자동 마이그레이션(PRD §11, 드라이버 modernc). 기본 주소 `127.0.0.1:48989`(`NEULSANG_ADDR`), DB 경로 `NEULSANG_DB_PATH`. 계층: domain(interface: capture/explain/inbox) ← db/sqlite·infra/llm/gemini(구현), transport/http/handlers.
백로그 #6 완료: 실제 AI provider(**Gemini**) 연동 — `internal/infra/llm/gemini` REST 클라이언트(`x-goog-api-key`, `responseSchema` 구조화 출력, `parseResponse` 검증), 재시도(20s/attempt, 최대 2회, 지수 백오프, 429/5xx/네트워크만), `raw_response_json` 보존. `Explainer.Explain`은 `(ExplainResult, rawResponseJSON, error)` 반환. **범용성**: `NEULSANG_AI_PROVIDER`(gemini/mock)로 선택, 미지정 시 API key 유무로 자동. **API key는 `NEULSANG_GEMINI_API_KEY` 환경변수로만 읽고 DB·파일·로그에 저장 안 함**(암호화 DB/OS 키체인은 #17 재검토, ADR-0004 부록). 기본 모델은 무료 티어가 가장 넉넉한 `gemini-flash-lite-latest`(`internal/infra/llm/gemini` DefaultModel), `NEULSANG_GEMINI_MODEL`로 override. 로컬 편의를 위해 `internal/config/dotenv.go`가 cwd에서 상위로 올라가며 `.env`를 찾아 로드(실제 환경변수 우선, `main`에서만 호출해 테스트 오염 방지, `.env`는 gitignore됨). 실 키로 전체 루프 E2E 검증 완료(검색→실제 한국어 해석→sub_items 추출→candidate→mark-unknown→review card→due 조회). 실 검증으로 드러난 AI 계약 견고화(`internal/infra/llm/gemini`): (1) flash-lite가 difficulty/importance를 0~1 밖으로 반환 → `parseResponse`에서 `clamp01` 정규화(안 하면 Validate 실패로 해석 폐기), (2) sub_items의 item_type 빈 값·candidate 미생성 → responseSchema에서 sub_items·review_card_candidates를 top-level `required`+`minItems:1`, item_type/card_type `enum`, sub_item 필수필드 `required` 강제 + 프롬프트 명시, (3) 빈 detected_language(soft 메타) → `und` 기본값(brief/detailed 같은 핵심 내용은 strict 유지). codex 리뷰 반영. mock은 항상 완벽한 sub_items라 이 문제들이 안 보였음 → 실 provider 검증의 가치.
백로그 #7 완료(2026-07-08): Knowledge/Learner item 추출(PRD Task05) — `internal/db/sqlite/explain_repo.go`의 `SaveSuccess` 트랜잭션 안에서 `extractKnowledge`/`upsertKnowledgeItem` 실행(explanation 저장과 원자적 커밋). AI `sub_items`를 `knowledge_items`로 `(normalized_key,item_type)` 병합(SELECT-then-INSERT/UPDATE, `first_seen_at` 보존·`last_seen_at` 갱신, 빈 key skip), `capture_items` 연결(role=`sub_item`, confidence=`importance`), `learner_items` `ON CONFLICT` upsert(`ask_count`+1, `last_asked_at`). codex 교차검토 반영: 결과 단위 sub_item dedup(이중 계상 방지), 재검색 빈 값은 `COALESCE`로 기존 pronunciation/meaning 보존, TOCTOU는 `SetMaxOpenConns(1)`로 안전. agy는 provider 쿼터 소진으로 미실행. 추출은 infra/sqlite에 국한(domain은 infra 무의존 유지).
백로그 #8 완료(2026-07-08): Mark-unknown API + review card candidate(PRD §15.4/§12.1/§14.3). **Part A**: 마이그레이션 `0002_review_card_candidates.sql`로 `review_card_candidates` 테이블 추가, `SaveSuccess` 트랜잭션 안에서 candidate를 capture+**primary knowledge_item**(importance 최고, sub_item 0개면 NULL)에 연결해 원자 저장(이전엔 `raw_response_json`에만 존재). **Part B**: `internal/domain/knowledge` 도메인 신설 — `POST /v1/knowledge/{id}/mark-unknown`(`learner_items` wrong_count+1·last_wrong_at·status=active)·`mark-known`(status=known), 없으면 404, 응답에 candidate_count 포함. `knowledge_repo.go`/handler/router(5번째 파라미터) 배선. codex 정확성 리뷰 통과, agy는 쿼터 소진으로 미실행(Claude 직접 아키텍처 검토 SOUND). **알려진 편차**: §14.3은 knowledge item upsert도 knowledge 도메인 소관이나, explanation과 원자적이어야 해 현재 sqlite explain repo(#7)에 위치 — 도메인 성장 시 분리 재검토. **DB 스키마: 마이그레이션 2개(0001 init, 0002 candidates)**.
백로그 #9 완료(2026-07-08): Review card 생성(PRD Task06). mark-unknown(#8)이 이제 해당 knowledge item의 unconsumed `review_card_candidates`를 `review_cards`로 **같은 트랜잭션에서 원자적으로** 생성(state=`new`, due_at=`now`, card_type 없으면 `meaning`), 응답에 `cards_created`. 멱등성: 마이그레이션 `0003`로 `review_card_candidates.consumed_at` 추가 — unconsumed만 소비, 재호출 시 카드 0개(wrong_count은 증가), 나중 캡처가 추가한 candidate는 다음 mark-unknown에서 카드화. `internal/domain/review` 도메인 신설(Card/Repository.DueCards/Service.Due limit 50·max 200) + `internal/db/sqlite/review_repo.go` + `GET /v1/reviews/due?limit=N`(due_at<=now soonest-first, answer 미노출; grading은 #10) + router 6번째 파라미터. codex 정확성 리뷰 통과, agy는 쿼터 소진으로 skip. **알려진 편차**: §14.3은 카드 생성이 review 도메인 소관이나 learner 변경과 원자적이어야 해 생성 SQL은 knowledge mark-unknown tx(sqlite)에 위치. **DB 스키마: 마이그레이션 3개(0001 init, 0002 candidates, 0003 consumed_at)**.
백로그 #21 Phase1(MVP) 완료(2026-07-09): 한글 발음→영어 후보 추론. 방향=하이브리드(AI 우선+캐시, Opus 외부조사 근거). Phase1은 순수 Gemini: `internal/domain/suggest`(Suggester·Service·Mock) + gemini `Suggest`(전용 프롬프트/스키마, confidence clamp) + `GET /v1/suggest?q=`(router 8번째). gemini Client가 Explainer·Suggester 둘 다 구현(공용 `generate` 헬퍼). 실 Gemini 검증: 스테일→stale/style/stall, 뮤텍스→mutex, 이디엠포턴트→idempotent, 카디널리티→cardinality. Phase3(KoG2P 퍼지)는 후속. **Phase2 완료(2026-07-09)**: 마이그레이션 0004 `suggest_cache` + `suggest_repo.go`(Cached/SavePick), Service 캐시 우선(hit→반환, miss→AI), `POST /v1/suggest/confirm`으로 확정 픽 저장(hit_count 누적), Candidate.Source(ai/cache). codex 리뷰 통과, E2E로 캐시 히트(AI 미호출) 확인. **DB 스키마: 마이그레이션 4개(0001~0004)**.
백로그 #22 완료(2026-07-09): review_card_candidate ↔ sub_item 매핑. AI 계약을 재구성해 candidate를 최상위가 아니라 **각 sub_item 안에 중첩**(`explain.SubItem.CardCandidates`, responseSchema `card_candidates` minItems:1). `extractKnowledge`가 sub_item 루프에서 그 knowledge_item에 직접 연결 → **비-primary 단어 mark-unknown도 카드 생성**(이전엔 primary만). 참조 무결성 문제 자체 소멸(Opus 외부조사 권장안). codex 리뷰 통과, 실 Gemini E2E로 비-primary 카드 생성 확인. 홀리스틱 리뷰 교차기능 결함 6건도 이 시점에 수정 커밋(mark-known 반영/inbox capture-scoped/SaveSuccess 실패 저장/shutdown WaitGroup/dashboard read-tx/rows-close).
백로그 #12 완료(2026-07-08, v0.3 백엔드 마일스톤 완료): Dashboard summary API(PRD Task08/§15.7). `stats` 도메인 신설(§14.3) — `internal/domain/stats` + `internal/db/sqlite/stats_repo.go`(7개 읽기 쿼리) + `GET /v1/dashboard/summary`(router 7번째 파라미터). 응답: today/week 검색수(UTC 오늘/rolling 7일), 오늘 완료 복습수, due card 수, most_searched·most_wrong(TOP 10), category_weakness(domain_category별 ask/wrong/mastery **평균**에 `review.WeaknessScore` 적용=평균 아이템 약점, codex 지적 반영해 합계→평균으로 편향/마스킹 제거, WeaknessScore float화). codex 리뷰(no blockers), agy skip. 백엔드 API(#1~#12)는 curl 검증 완료, 실 Gemini 해석까지 확인.
백로그 #13 완료(2026-07-10, v0.4 프론트엔드 트랙 시작): Tauri 셸 부트스트랩. `apps/desktop-ui` Tauri 2 + React 19 + Vite + TS 스캐폴드(module `neulsang`, identifier `com.dgkwon90.neulsang`). `src-tauri/src/tray.rs`=트레이 메뉴(Quick Search/Inbox/Today Review/Dashboard/Settings/Quit, PRD §10.1)→메인 윈도우 show/focus + `navigate` 이벤트(**라벨** emit), Quit=`app.exit(0)`. `sidecar.rs`=desktopd 자식 프로세스 spawn/kill(바이너리 탐색 `NEULSANG_DESKTOPD_BIN`→실행파일옆→dev `../../desktopd/desktopd`, 없으면 UI 단독 기동), `Mutex<Option<Child>>`. `lib.rs`=`RunEvent::Exit`에서 사이드카 정리, tauri-plugin-log/http/opener 등록. 프론트: `src/App.tsx`(navigate 리스너로 화면 전환+헬스 폴링, 라우트=트레이 라벨), `src/api/client.ts`(`DesktopdClient`, 기본 `127.0.0.1:48989`, **`@tauri-apps/plugin-http` fetch로 CORS/mixed-content 우회** — desktopd에 CORS 안 엶). codex 리뷰 반영: 트레이 id→라벨 emit(안 하면 네비게이션 전무=수용기준 실패), 브라우저 fetch→plugin-http, capability 슬림(Rust 직접호출은 capability 무관, http만 `127.0.0.1:48989/*` 스코프), 트레이 아이콘 non-panic. 검증: `cargo check`/`clippy -D warnings`/`fmt --check`, `npm run build` 통과(GUI 수용기준=트레이 클릭 시 화면 전환은 `npm run tauri dev`로 사용자 수동 확인 필요). **부모-사망 watchdog(2026-07-10 추가)**: `internal/watchdog` — 셸이 `NEULSANG_PARENT_PID`로 게이트, desktopd가 `os.Getppid()` 재입양(부모 PID 변화)을 2초 폴링 감지 시 종료 컨텍스트 취소(SIGINT와 동일 graceful). 셸 비정상 종료 시에도 고아 방지. PID 재사용 안전(재입양 감지). codex no blockers. Windows는 재입양 없어 미동작(Job Object로 후속, macOS 우선).
백로그 #14 완료(2026-07-10): 글로벌 단축키 + Quick Search 팝업. Quick Search를 프레임리스 팝업 윈도우(`quicksearch`, tauri.conf.json visible:false·decorations:false·alwaysOnTop·skipTaskbar·center)로 분리 — `tauri-plugin-global-shortcut`으로 `CommandOrControl+Shift+E`(mac Cmd/그 외 Ctrl) 등록(`lib.rs`, 실패해도 앱 기동 유지=경고만), 트레이 "Quick Search"도 `popup::show` 공유. 팝업 활성화 시 `quicksearch:activate` 이벤트→클립보드 자동삽입·포커스. 프론트: `main.tsx`가 `getCurrentWindow().label`로 렌더 분기(main=App 탭 Inbox/Review/Dashboard/Settings, quicksearch=`QuickSearch.tsx`), QuickSearch가 입력→`POST /v1/captures`(input_mode clipboard/manual 판별)→`GET .../explanation` 폴링(700ms/90s, 세대번호 취소, await 후 재확인)→결과 표시, Esc 숨김. `tauri-plugin-clipboard-manager`, capability `clipboard-manager:allow-read-text`·`core:window:allow-hide` 창 스코프 `["main","quicksearch"]`. codex 리뷰 반영(단축키 등록 실패 non-fatal, 폴링 stale-guard, Cargo target 배치 수정). 검증 `cargo check`/`clippy -D warnings`/`fmt`, `npm run build` 통과.
백로그 #15 완료(2026-07-10): Inbox 화면. **선행 백엔드**: 단어별 모름이 knowledge_item id를 요구하나 노출 API가 없어 `GET /v1/captures/{id}/knowledge` 신설(knowledge 도메인 `CaptureItem`/`ListByCapture`/`ErrCaptureNotFound` + sqlite capture_items↔knowledge_items↔learner_items 조인, capture 없으면 404·있으나 항목 0이면 200 빈목록, test-race/golangci 통과). **프론트**: `src/inbox/Inbox.tsx` 5탭(new/saved/review_added/archived/failed)→`GET /v1/inbox?status=`, 행 펼치면 추출 단어별 **모름**(mark-unknown, `cards_created`로 "복습 카드 N개 생성")·**알아요**(mark-known), 저장(new만)/보관 액션. `api/client.ts`에 listInbox/save/archive/listCaptureKnowledge/markUnknown/markKnown+타입. codex 리뷰 no blockers(단어 모름 후 캡처 파생상태 변화 즉시 미반영은 멀티단어 마킹 보존 위해 의도적 MVP). 검증 `npm run build`. **Esc 팝업 미닫힘 버그(#14)도 이 시점 수정**(document 레벨 keydown). **다음 작업: #16 Review 화면(depends #13,#10) — 카드 표시→답변→Again/Hard/Good/Easy→다음 카드. `GET /v1/reviews/due`·`POST /v1/reviews/{id}/grade` 연동.**
백로그 #16 완료(2026-07-11): Review(Today Review) 화면. **선행 백엔드 계약 변경**: `GET /v1/reviews/due`(+`session/start`) 응답에 `answer`/`explanation` 추가 — 자가 채점 복습에 답이 필요한데 Neulsang은 로컬 단일 사용자라 유출/치팅 개념이 없어 별도 reveal 왕복 대신 배치로 내려줌(기존 "answer 미노출" 가드는 소비자 없던 시점의 것 → 뒤집음). `review.Card`에 Answer/Explanation, `DueCards` SELECT/Scan(explanation NULL→`sql.NullString`), `reviewCardResponse` JSON(answer 항상, explanation `omitempty`). PRD §15.5에 계약 참고 명시. **프론트**: `src/review/Review.tsx` 세션 상태머신(loading/error/empty/active/done) — 질문만 표시→"답 보기"(Space)→답/설명 공개→Again/Hard/Good/Easy(키 1~4) 채점→다음 카드, 진행(n/N)·완료 요약. `client.ts` `dueReviews`/`gradeReview`+타입(ReviewCard/ReviewRating/GradeResult), `App.tsx` "Today Review" 라우트 배선. elapsed_ms=카드표시→채점 경과(서버 `>=0` 검증과 정합). codex 교차검토 no blockers(scan 순서/NULL·중복채점 `grading` ref 가드·키보드 리스너 생명주기·훅 deps 확인). MINOR 문서드리프트만 PRD 반영, grade 응답 미사용·에러 시 전체 리로드는 의도된 MVP. 검증 `go test -race ./...`/`golangci-lint`(0 issues)/`npm run build` 통과. **다음: #17 Dashboard+Settings(depends #12,#14,#16) — sidecar발 이벤트(결과준비·복습시간) UI 인지 방식 설계 결정을 착수 시 먼저 문서화(되돌리기 어려우면 ADR).**
백로그 #11 완료(2026-07-08): mastery/weakness score(PRD §13.2~13.3). `internal/domain/review/scoring.go` 순수함수 `MasteryScore`(clamp 0..1)·`WeaknessScore`(0 floor). `Grade` 트랜잭션에서 review_log 삽입 후 `gradeCountsForKnowledgeItem`(item의 모든 카드 review_logs를 `source='review'` 필터로 rating별 집계)으로 전체 이력 재계산 → `learner_items.mastery_score` upsert(응답에 mastery_score 포함). weakness는 컬럼 없어 파생값(정렬/#12 대시보드용, recent_repeat_bonus MVP 0). codex CLEAN 리뷰(source 필터·다중카드 테스트 반영), agy는 여전히 쿼터 소진 skip.
백로그 #10 완료(2026-07-08): Review session + FSRS-lite(PRD Task07/§13.1/§15.5·15.6). FSRS-lite 코어는 `internal/domain/review/schedule.go` 순수함수 `NextSchedule`(첫복습 초기간격 Again10분/Hard1일/Good3일/Easy7일, 이후 ×1.2/×2.5/×4.0, **Again은 reps=0 리셋=재학습**). `POST /v1/reviews/{id}/grade`→sqlite `Grade` 트랜잭션(카드 읽기→NextSchedule→review_cards 갱신+`review_logs` append-only+`learner_items` review_count upsert, `stability`=현재 간격일수, 없는 카드 404). `POST /v1/reviews/session/start`=due 목록. mastery/weakness는 #11. codex 리뷰 통과, agy skip.
개발 워크플로(2026-07-12): `npm run tauri dev`의 `beforeDevCommand`에 `build:sidecar` 추가 — 기동 시 Go `desktopd`를 자동 재빌드해 백엔드 변경분을 매번 반영(이전엔 dev가 프론트만 갱신하고 구버전 사이드카를 spawn해 새 API 필드가 안 내려오는 함정이 있었음). Go 미설치 시 경고만 하고 UI 단독 기동.
백로그 #17 완료(2026-07-12, v0.4 프론트엔드 트랙 Dashboard/Settings): PRD §10.6/§10.7/§15.7/§15.8. **설계 결정(착수 전 확정, ADR-0004 부록)**: 설정을 두 config 계층으로 분리 — **(1) `.env`** = 부트스트랩/저빈도 시작 옵션(DB 경로·주소·AI provider·모델·**API key**)은 Settings에서 읽기전용 표시, **(2) `app_settings`(DB)** = 동작 정책·운영 튜닝(알림 허용·아침/저녁 복습 시간)은 편집·저장. `app_settings`는 평문 정책 저장소라 secret 미포함 → **API key 저장 재검토 결론=env-only 유지**(#6 결론 재확인, 키체인/암호화 DB는 별도 보안 이슈로 defer). **백엔드**(마이그레이션 불필요, `app_settings`는 0001에 존재): `internal/domain/settings`(Preferences/Validate/Service, infra 무의존) + `db/sqlite/settings_repo.go`(app_settings KV upsert, Load는 Defaults로 미설정 키 채움) + `handlers/settings.go`(`GET/PUT /v1/settings`, 응답=편집 `preferences`+읽기전용 `effective`, **API key는 `api_key_configured` bool만 노출**, PUT=full replace라 잘못된 HH:MM은 400·저장 안 함) + bootstrap `effectiveConfig()`/`resolvedProvider()`(mock provider면 gemini_model 빈값) + 라우터 9번째 파라미터. **프론트**: `src/dashboard/Dashboard.tsx`(=#12 `GET /v1/dashboard/summary` 소비, 지표·TOP단어·카테고리 약점 막대, 읽기전용), `src/settings/Settings.tsx`(preferences 편집→PUT + effective 읽기전용, API key 유무만), `api/client.ts` dashboardSummary/getSettings/updateSettings+타입, `App.tsx` 라우트 배선. Rust/capability 변경 없음(http 스코프 `127.0.0.1:48989/*`가 PUT 커버). codex 교차검토 no blockers(API key 무유출·PUT full-replace·계층 검증), 지적한 mock provider 모델 표시 nit 반영. 검증: `go test -race`/`vet`/`golangci-lint`(0 issues), curl E2E(기본값+effective·저장·400·**API key 값 미노출**), `npm run build`. **범위 밖(후속)**: 최근 7일 추세 차트(요약 API에 시계열 없음), 단축키 런타임 커스터마이즈(Rust global-shortcut 재등록), 인프라 런타임 편집. 백업/동기화/계정연결은 #19·#20. **DB 스키마: 마이그레이션 4개(0001~0004) 유지**. **다음: #18 알림·리마인더(depends #14,#16) — 착수 시 sidecar발 이벤트(결과 준비·복습 시간) UI 인지 방식(폴링/SSE/OS 알림) 설계 결정을 먼저 문서화(PRD 갭3, 되돌리기 어려우면 ADR).**
